// Define constant in this file

const HOST_SITE = ''

// 定义状态码
const SUCCESS = 2000
const TOKEN_EXPIRED = 2001

// TOKEN
const TOKEN = '2Dat38zQ1d0vBnSMx1'

// 定义每一页数据
const PAGE_ROWS = 15

// 文件上传路径
const FILE_UPLOAD_PATH = HOST_SITE + '/file/upload'
// 富文本上传路径
const EDITOR_UPLOAD_PATH = FILE_UPLOAD_PATH
const FILE_UPLOAD_PATH_EX = HOST_SITE + '/file/upload-file'

export {
  HOST_SITE, SUCCESS, TOKEN_EXPIRED, PAGE_ROWS, EDITOR_UPLOAD_PATH, FILE_UPLOAD_PATH, FILE_UPLOAD_PATH_EX
}